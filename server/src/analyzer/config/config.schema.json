{
  "title": "RestServicesSchema",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string"
      },
      "baseUri": {
        "type": "string"
      },
      "language": {
        "type": "string",
        "enum": [ "Typescript", "Java", null ]
      },
      "lib": {
        "type": "string",
        "enum": [ "NestJS", "JavaSpark", null ]
      }
    },
    "if": {
      "properties": { "language": { "const": "Typescript" } }
    },
    "then": {
      "properties": { "lib": { "enum": [ "NestJS" ] } }
    },
    "else": {
      "properties": { "lib": { "enum": [ "JavaSpark" ] } }
    },
    "required": [ "name", "baseUri", "language", "lib", "endpoints" ]
  }
}
